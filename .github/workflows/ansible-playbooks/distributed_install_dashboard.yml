---
- hosts: all
  gather_facts: false
  become: true

  vars:
    tmp_path: "{{ tmp_path }}"

  tasks:
    - name: Install Wazuh dashboard
      command: "bash wazuh-install.sh -wd {{ inventory_hostname }} -v"
      args:
        chdir: "{{ tmp_path }}"
      register: dashboard
